# Recorder application

The working recorder entry point remains `daybreak-recorder`. Its source is in `daybreak_recorder/`. A later release will migrate it into the unified namespace without breaking stored event schemas.
